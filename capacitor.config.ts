import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bantaybayan.app',
  appName: 'Bantay Bayan System',
  webDir: 'dist'
};

export default config;
